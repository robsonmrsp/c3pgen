
- Correções a serem aplicadasque foram percebidas durante o desenvlvimento do FormOrders no AllDelivery

  Veja o Commit fb023da239c9ecf7b9ea20b677d438401af707e7 do alldelivery
 - Correção do layout do JsetupDataTable Ver em frameItemOrder
 - Ajuste no JdataTable para exibir ou não o counter e paginator(ver GRidTemplate)
 - criação de css para ajuste de layout(ver custom.js)
 
 - implementação do NumberCell para receber atributos compostos 
	var NumericCell = Backgrid.Cell.extend({
		className : "custom-number-cel",
		type : 'decimal',
		render : function() {
			this.$el.empty();
			var model = this.model;
			var theColValue = null;

			if (this.column.get("name").indexOf('.') > 0) {// composto
				var fields = this.column.get("name").split('.')
				theColValue = parseFloat(model.get(fields[0])[fields[1]]);
			} else {
				theColValue = parseFloat(model.get(this.column.get("name")));
			}
Fim do commit fb023da239c9ecf7b9ea20b677d438401af707e7









 29/04/2016 
   #colocar o pluguin de decimal no sistema substituindo o atual
    http://plentz.github.io/jquery-maskmoney/
   
   #Para o duturo trabalhar com o javaDateTime do java8
   #Atualizar as bibliotecas utilizadas. Spring, Hibernate, Jackson.

   #estilizar o modal que aparece quando se vai excluir um elemento do grid. Fazer como está p
   melhorar o combobox para que ele possa rodar um loading...
   
   
   
   sobre scroll
   https://github.com/web-design-weekly/scroll-to-position-demo/blob/master/js/main.js
   
   
   fazer o tratamento de erro do etiquetar e do deltaweb